# LingoGrove 动词变位测验 —— 实施方案（延后执行，待词库达标自动触发）

**背景**：同 WageLark/DialWick 那两份文档，2026-08-04 调研发现单一事实查询页对 AI Overview 最脆弱，LingoGrove 的动词变位表页面(`Conjugations` 分类)正是这种格式。这个工具把静态变位表变成需要用户答题互动的测验，天然抗 AI 摘要替代。

**为什么延后**：截至 2026-08-04，`src/data/guides.ts` 里只有 1 个动词的变位表（ser）。测验题库只有1个动词，出不出多样的题，用户体验太单薄，现在做性价比不高。**触发条件：`Conjugations` 分类的动词条目数达到 10 个（含）以上时再做**。`lingogrove-content-publishing` 任务每次运行会自动检查这个数字，达标后会按本文档自动实施，不需要人再回来手动触发。

## 功能描述

用户选择一个已有变位表的动词（或从某篇 Conjugations 文章页面直接进入该动词的测验），工具随机抽取"动词+时态+人称"组合出题（比如"conjugate necesitar in the imperfect for tú"），提供多选项（1个正确答案+2-3个从同一动词其他时态/人称里抽取的干扰项，不编造错误变位形式），用户选择后即时反馈对错，测验结束显示得分。

## 数据与实施

数据源：`src/data/guides.ts` 里 `category: 'Conjugations'` 的文章的 `conjugation` 字段（`ConjugationTable` 结构：`verb`/`language`/`tenses[]` → `forms[{pronoun, form}]`），由 `tools/conjugate.mjs` 生成，是权威、经过单元测试验证的数据，不需要重新核实。

1. `src/lib/quizGenerator.ts` —— 纯函数，输入一个或多个 `ConjugationTable`，输出随机抽样的题目列表（题干+正确答案+干扰项，干扰项必须来自真实存在的其他 form，不能编造）。配套单元测试确保干扰项不会跟正确答案重复、题目覆盖不同时态/人称。
2. `src/components/ConjugationQuiz.astro` —— vanilla `<script type="module">` island（不新增框架依赖），选择题UI+计分。
3. 接入位置：每篇 Conjugations 文章页面底部加"练习这个动词"入口；如果当时词库已经有多个动词，额外做一个 `/conjugations/quiz` 汇总页，可以跨动词混合出题。

## 验收标准

同 WageLark/DialWick 那两份文档：`npm test`全绿 + `npm run build`无错误 + 手动验证 + commit/push + 日志记录，说明是产品功能不是文章。

## 触发与执行说明（写给 lingogrove-content-publishing 任务）

见 `~/.claude/scheduled-tasks/lingogrove-content-publishing/SKILL.md` 里的对应检查步骤——每次任务运行时顺手检查 `Conjugations` 条目数，达到 10 个后按本文档实施一次，实施完成后从 SKILL.md 里删除那条检查指令（避免以后每次运行都重复检查/重复实施）。
