# LingoGrove "选词填空"小测 —— 实施方案

**要解决的问题**：跟 DayAlmanac 同一批动机——DayAlmanac 和 LingoGrove 是流量站矩阵十站里唯二零互动元素的站，内容全是"纯查询即可满足"的静态参考信息，对AI Overview一句话答完、零点击的风险最脆弱。DayAlmanac 已经上线了 true/false 判断题（复用FAQ数据）；LingoGrove 内容形态不一样（语言语法对照，不是"是否/真假"型事实），不能照搬同一种题型，改用更贴合语言学习场景的"选词填空"机制，也避免矩阵内游戏同质化（跟FactCrumbs猜数字同理）。

## 数据来源与选题

不新写例句，不做正则抽取。通读四篇"易混淆词对"语法文章（por-vs-para / ser-vs-estar / preterite-vs-imperfect / saber-vs-conocer——这四篇是全站18篇里唯一天然适合"二选一"题型的，其余语法文章是单一主题讲解不是对照，跳过）正文里已发表的西语例句，把目标词/词形挖空，选项就是这对易混淆词本身，解释文字取自文章原文对该例句的讲解（不改写、不新造论断）。ser-vs-estar 和 preterite-vs-imperfect 两篇文章自己就给出了"同句换词/换时态、含义完全不同"的对照例句（es/está simpática，es/está aquí，llegué/dormía），这类天然对照题优先收录。

23条，覆盖4篇文章。

## 数据结构 + 组件

跟 DayAlmanac `DayQuiz`（true/false）同一套基础设施（Fisher-Yates洗牌+markdown渲染），但交互形态改成二选一填空：

- `src/data/wordQuiz.ts`：`WordQuizItem[]`，字段 `{sentence, translation, choices: [string,string], correctIndex, explanation, sourceSlug, sourceTitle}`——`sentence` 里用 `___` 标记挖空位置
- `src/lib/wordQuiz.ts`：`pickQuizSet()`（跟DayAlmanac同一份Fisher-Yates实现）+ 单元测试
- `src/components/WordQuiz.astro`：vanilla `<script>` island（不写`type="module"`），视觉复用LingoGrove既有 `.panel`/`.rubric` 样式 + `--flag`（森林绿）/`--flag-dark`/`--ink`/`--paper`/`--rule` 变量
- 新建 `/fill-in-the-blank/` 页面

## 验收标准

- 每条sentence/translation/explanation能在对应文章正文逐字核对到（挖空本身是机械编辑，不算新增内容）
- `npm test` 全绿（package.json的test脚本glob需要扩到同时跑 `tools/**/*.test.mjs` 和 `src/lib/*.test.ts`）
- `npm run build` 无错误
- 手动/自动验证完整一轮流程
- 完成后 commit + push + 确认线上生效 + IndexNow提交 + 内容发布日志追加记录
